# ava-solution-site

Статический сайт ТОО «AVA Solution» (ava-solution.kz).
Содержимое этого репозитория разворачивается в `httpdocs` через Git-деплой Plesk (PS.kz).

- Языки: RU (корень), KZ (`/kk/`), 中文 (`/zh/`)
- `send.php` — обработчик формы заявки
- `.htaccess` — правило страницы 404
